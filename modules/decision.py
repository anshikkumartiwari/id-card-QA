def make_decision(results):
    # Placeholder Logic
    # In reality, check weights from config and results from other modules
    
    score = 0.0
    # Simple aggregation for now
    return {"decision": "ACCEPT", "score": score}
